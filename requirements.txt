# Python 3.11
# EC2 OS is chosen to fit these pins (Amazon Linux 2023). Do not downgrade
# this file to run on AL2. See aws-pgx-setup/ec2/README_os_and_libraries.md.
# Core data and SHAP/FFA workflow (DuckDB for memory-efficient CSV/parquet)
duckdb>=1.4.0

# Data processing
pandas>=3.0.0
numpy>=2.0.0
pyarrow>=23.0.0
openpyxl>=3.1.0

# AWS
boto3>=1.28.0

# Models and explainability (Steps 6–8)
scikit-learn>=1.8.0
joblib>=1.3.0
catboost>=1.2.0
xgboost>=2.0.0
optuna>=3.0.0

# Plotting and FFA
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.18.0
networkx>=3.0

# NLP and text analysis (Cohort PGx network topology)
# After pip install, run: python -m spacy download en_core_web_sm
spacy>=3.7.0
pytextrank>=3.2.0
beautifulsoup4>=4.12.0

# DTW alignment (Step 9 dashboard visuals; required, not optional)
dtaidistance>=2.3.0

# Utilities
tenacity>=8.0.0
certifi>=2023.0.0

# Imported at module load by py_helpers.common_imports and aws_utils.
# Cohort/SES jobs fail before any pipeline work if these are missing.
mlxtend>=0.23.0
psutil>=5.9.0
jinja2>=3.1.0
requests>=2.31.0
