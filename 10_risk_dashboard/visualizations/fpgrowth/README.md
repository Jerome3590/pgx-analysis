# FP-Growth visualization outputs

This directory contains **outputs only** for FP-Growth dashboard visualizations (itemsets, rules, plots, network HTML).  
**Creation code** lives in `9_dashboard_visuals/fpgrowth/` (pipeline step 9). Do not add scripts here; follow the same pattern as `6_final_model`, `7_shap_analysis`, and `8_ffa_analysis` (code in step folder, outputs in designated output location).
