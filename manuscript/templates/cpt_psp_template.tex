%% Pandoc/Quarto LaTeX template for Wiley CPT: Pharmacometrics & Systems Pharmacology (PSP)
%%
%% Used via:  format: wiley-njd-pdf:  (extension bundles WileyNJDv5.cls + .bst files automatically)
%%   template: ../templates/cpt_psp_template.tex   <- add this line to the wiley-njd-pdf block
%%
%% Citation style : AMA  (American Medical Association – numeric superscript)
%% Font / layout  : STIX 2-column  (standard for CPT:PSP)
%% ============================================================

\documentclass[$for(classoption)$$classoption$$sep$,$endfor$]{WileyNJDv5}

%% ---- Packages required by Pandoc-generated LaTeX ----
\usepackage{calc}       % provides \real{} for longtable column width arithmetic
\usepackage{longtable}  % Pandoc renders markdown tables as longtables
\usepackage{booktabs}   % \toprule, \midrule, \bottomrule
\usepackage{array}      % extended column types in tabular
\usepackage[section]{placeins} % prevent floats drifting past section boundaries
%% Aggressive float placement: prefer inline over float pages
\renewcommand{\floatpagefraction}{.9}
\renewcommand{\textfraction}{.05}
\renewcommand{\topfraction}{.9}
\renewcommand{\bottomfraction}{.7}
%% Typography: fewer overfull lines and better hyphenation in narrow 2-column measure
\usepackage{microtype}
\AtBeginDocument{%
  \hyphenation{phar-ma-co-e-pi-de-mi-o-log-i-cal e-pi-de-mi-o-log-i-cal
    inter-pret-a-bil-i-ty phar-ma-co-vi-gi-lance}}

%% ---- First-page vertical glue (CH3/CTS-style tightness, but STIX2COL) ----
%% Clinical CTS chapters use STIX1COL + cts_template.tex: one-column title page,
%% no Wiley two-column abstract strip. CPT:PSP uses STIX2COL; WileyNJDv5's
%% \absworkarea inserts \vspace*{2pc} after the title/abstract strip (six times),
%% which reads as extra whitespace on page 1. Shrink that glue; keep 2-col body.
\usepackage{xpatch}
\makeatletter
\adjtitleskip=0pt plus 2pt minus 1pt
\@ifundefined{absworkarea}{%
  \PackageWarning{cpt_psp_template}{absworkarea missing; spacing patch skipped}}{%
  \xpatchcmd*\absworkarea{\vspace*{2pc}}{\vspace*{0.5pc}}{}{%
    \PackageWarning{cpt_psp_template}{xpatch on absworkarea failed}}%
}
\makeatother

%% longtable fails in 2-column mode (STIX2COL). Patch: temporarily switch to 1-column.
\makeatletter
\let\@orig@longtable\longtable
\let\end@orig@longtable\endlongtable
\renewenvironment{longtable}{%
  \if@twocolumn\@restonecoltrue\onecolumn\else\@restonecolfalse\fi
  \@orig@longtable
}{%
  \end@orig@longtable
  \if@restonecol\twocolumn\fi
}
\makeatother

%% ---- Pandoc/Quarto compatibility shims (BEFORE header-includes) ----
%% \tightlist required by Pandoc compact lists
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
%% WileyNJDv5 may pre-define \listoflistings via newfloat; undefine so Quarto can redefine it
\let\listoflistings\relax

$if(header-includes)$
$header-includes$
$endif$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

%% ---- Article metadata ----
\articletype{$if(article-type)$$article-type$$else$Research Article$endif$}
\received{$if(received)$$received$$else$$endif$}
\revised{$if(revised)$$revised$$else$$endif$}
\accepted{$if(accepted)$$accepted$$else$$endif$}
\journal{$if(journal)$$journal$$else$CPT: Pharmacometrics \& Systems Pharmacology$endif$}
\volume{$if(volume)$$volume$$else$00$endif$}
\copyyear{$if(copy-year)$$copy-year$$else$$if(year)$$year$$else$2026$endif$$endif$}
\startpage{1}
\raggedbottom

\input{../templates/wiley_firstpage_mdpi_like.tex}

%% ---- Title ----
$if(title)$
\title{$title$}
$endif$

%% ---- Authors (Quarto native format: $by-author$ / $by-affiliation$) ----
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$]{$by-author.name.literal$}
$endfor$

\authormark{$if(author-mark)$$author-mark$$else$$for(by-author)$$by-author.name.literal$$sep$, $endfor$$endif$}
$if(title)$
\titlemark{$if(title-mark)$$title-mark$$else$$title$$endif$}
$endif$

$for(by-affiliation)$
\address[$it.number$]{%
  $if(it.department)$\orgdiv{%
    $it.department$%
  }\par$endif$%
  \orgname{$if(it.name)$$it.name$$endif$}\par
  \orgaddress{%
    $if(it.city)$\city{$it.city$}$endif$%
    $if(it.region)$, \state{$it.region$}$else$$if(it.state)$, \state{$it.state$}$endif$$endif$%
    $if(it.country)$, \country{$it.country$$endif$}%
  }%
}
$endfor$

%% ---- Correspondence ----
$for(by-author)$
$if(by-author.attributes.corresponding)$
\corres{$by-author.name.literal$\email{$by-author.email$}}
$endif$
$endfor$

%% ---- Abstract ----
\abstract[Abstract]{$abstract$}

%% ---- Keywords ----
$if(keywords)$
\keywords{$for(keywords)$$it$$sep$, $endfor$}
$endif$

%% ---- Journal citation line ----
\jnlcitation{\cname{%
$for(by-author)$\author{$by-author.name.literal$}$sep$, $endfor$}.
\ctitle{$title$.}
\cjournal{\it $if(journal)$$journal$$else$CPT: Pharmacometrics \& Systems Pharmacology$endif$}
\cvol{$if(year)$$year$$else$2026$endif$;$if(volume)$$volume$$else$00$endif$($if(issue)$$issue$$else$00$endif$):$if(pages)$$pages$$else$1--20$endif$}.}

%% ---- \maketitle: match standard article.cls twocolumn branch ----
%% WileyNJDv5 sets BOTH branches to \twocolumn[\@maketitle]. The kernel's
%% \@twocolumn begins with \clearpage, which ships an empty first page when
%% \maketitle is the first output. See \input file (Pandoc chokes on \@ in templates).
\input{../templates/cpt_psp_maketitle_patch.tex}

%% ============================================================
\begin{document}

\maketitle

%% After title/abstract strip so the footnote sits above the page footer, not fighting column 1.
$if(abbreviations-note)$
\renewcommand\thefootnote{}
\footnotetext{\textbf{Abbreviations:} $abbreviations-note$}
\renewcommand\thefootnote{\fnsymbol{footnote}}
\setcounter{footnote}{1}
$endif$

$body$

%% Style is set once by WileyNJDv5.cls (\bibliographystyle{WileyNJD-AMA} when AMA).
%% Do not repeat \bibliographystyle here — duplicate \bibstyle in .aux breaks BibTeX.
\bibliography{../refs/discipline,../refs/cpt-psp}

\end{document}
