# VCU Library Credentials
# Copy this file to secrets/secrets.txt and fill in your credentials.
# The secrets/ folder and secrets.txt are gitignored — they will NEVER be committed.

username=your_vcu_eid_here
password=your_vcu_password_here

# EZProxy base URL (VCU library proxy server)
# Leave as-is unless VCU changes their proxy address.
proxy_base=https://proxy.library.vcu.edu/login?url=

# After your first run, session_cookies.json will be saved to secrets/
# so Duo 2FA only needs to be completed once per session.
