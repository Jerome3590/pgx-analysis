# Dashboard visuals tests moved

All dashboard visuals tests (BupaR, DTW, FP-Growth prerequisite and outputs) now live under **11_testing/tests/dashboard_visuals/**.

Run from repo root:

```bash
pytest 11_testing/tests/dashboard_visuals/ -v
```

Or run all tests (dashboard + dashboard_visuals):

```bash
pytest 11_testing/tests/ -v
```

See **11_testing/README.md** and **11_testing/tests/README.md**.
